# Optional: merge into ASIN stream resolution for `em-tools amazon products index-asins --config ...`
# (same block as amazon products filter).

amz.uploadable_filter.asin_stream:
  time_field: auto
  cutoff: null
  label: null
  label_field: label
