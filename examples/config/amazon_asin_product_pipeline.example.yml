# Optional: merge into ASIN stream resolution for `em-tools amz-uploadable:asin-to-es --config ...`
# (same block as amz-uploadable:filter).

amz.uploadable_filter.asin_stream:
  time_field: auto
  cutoff: null
  label: null
  label_field: label
