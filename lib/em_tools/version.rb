# frozen_string_literal: true

module EmTools
  # Identifier for the running tree. Bumped manually whenever the platform's
  # external behaviour (CLI surface, ES schema, scheduled jobs) changes in a
  # way operators care about.
  VERSION = "0.1.0"
end
