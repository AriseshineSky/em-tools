#!/usr/bin/env bash
set -euo pipefail

# Bootstrap a fresh checkout of em-tools.
#
#   bin/setup

cd "$(dirname "$0")/.."

bundle install

if [[ ! -f .env ]]; then
  cp .env.example .env
  echo "[bin/setup] copied .env.example -> .env (please edit and fill in real values)"
fi

mkdir -p log tmp
